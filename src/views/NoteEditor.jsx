import React from "react";
import Loading from "../components/Loading";
import { useAuth0 } from "../auth/react-auth0-spa";

const NoteEditor = () => {
    return (
        <>
            This is the the Note Editor.
        </>
    );
};

export default NoteEditor;
